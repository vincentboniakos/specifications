<% if object.errors.any? %>
<div class="alert-message block-message error fade in">
    <a class="close" href="#">×</a>
    <p>
		<strong>Ooops ! You got <%= pluralize(object.errors.count, "error") %>.</strong> There were problems with the following fields:
    </p>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
</div>
<% end %>
